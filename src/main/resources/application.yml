spring:
  datasource:
    url: jdbc:mysql://localhost:3306/study
    username: root
    password: psb011031!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        show_sql: true
        format_sql: true
    hbm2ddl:
      auto: update
  profiles:
    active: prod

alarm:
  slack:
    webhook-url: ""     # 기본값: 비어 있거나 placeholder
  discord:
    webhook-url: ""
